{
  "name": "strokealert",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:api & npm run dev:web",
    "dev:api": "npm run dev --prefix apps/api",
    "dev:web": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix packages/shared && npm run build --prefix apps/api && npm run build --prefix apps/web",
    "start": "npm run start:api & npm run start:web",
    "start:api": "npm run start --prefix apps/api",
    "start:web": "npm run start --prefix apps/web",
    "lint": "npm run lint --prefix apps/web",
    "type-check": "npm run type-check --prefix apps/api && npm run type-check --prefix apps/web"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=18.0.0"
  }
}
